# Pinned at scaffold time — review and bump before first real run.
# Some versions deliberately tracking-latest because the May-2026 features
# need the most recent transformers / accelerate.

# Core HF stack
# transformers >=5.5 is required for the SDPA mem_efficient kernel to accept
# the attention-mask shape the Qwen3 model code passes in. Older versions
# (Kaggle ships 5.0.0 as of 2026-06) make the SDPA selector fall through to
# the math kernel which materialises the full N×N attention matrix and OOMs
# at >=8K context. See the LOCAL_KAGGLE_V1 sweep report for the trace.
transformers>=5.5.0
accelerate>=1.2.0
peft>=0.14.0
safetensors>=0.4.5
sentencepiece>=0.2.0
tokenizers>=0.20.0

# Performance kernels (CUDA only — skipped on non-CUDA stages)
# flash-attn>=2.7.0  ; sys_platform != "darwin"  # install via separate cell on Kaggle
# deepspeed>=0.15.0  ; sys_platform != "darwin"

# Eval / data
datasets>=3.0.0
evaluate>=0.4.3
numpy>=2.0.0
pandas>=2.2.0
scipy>=1.13.0

# Llama.cpp leg (for cells 9a/9b/9c)
llama-cpp-python>=0.3.0

# Oracle / API
anthropic>=0.40.0  # fallback path when claude CLI isn't authed
requests>=2.32.0

# Notebook UX
ipywidgets>=8.1.0
tqdm>=4.66.0
matplotlib>=3.9.0

# Git automation from the notebook
gitpython>=3.1.0

# --- Kaggle compatibility pins ---
# Kaggle's preinstalled rapids stack (cudf, cuml, dask-cuda, ucxx) pins numba
# below 0.62 and numba-cuda below 0.23. We don't use numba ourselves, but
# transitive deps from datasets/scipy can bump it. Hold it down so the
# preinstalled GPU-DataFrame stack stays consistent and pip stops warning.
numba<0.62
numba-cuda<0.23,>=0.22.1
